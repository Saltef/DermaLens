# Local Data Directory

Put downloaded datasets here, but do not commit medical images or private photos.

Recommended layout:

```text
data/raw/
data/processed/
```

The app does not need this folder to run. It is only for training/export workflows.

